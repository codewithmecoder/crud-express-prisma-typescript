version: "3"
services:
  # server:
  # image: postgrest/postgrest
  # ports:
  #   - "3000:3000"
  # links:
  #   - db:db
  # environment:
  #   PGRST_DB_URI: postgres://test:secret@db:5432/postgres
  #   PGRST_DB_SCHEMA: public
  #   PGRST_DB_ANON_ROLE: test #In production this role should not be the same as the one used for the connection
  #   PGRST_SERVER_PROXY_URI: "http://127.0.0.1:3000"
  # depends_on:
  #   - db
  db:
    image: postgres
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: test
      POSTGRES_PASSWORD: secret
      PGDATA: /var/lib/postgresql/data
    volumes:
      - db-data:/var/lib/postgresql/data

  pgadmin:
    container_name: pgadmin4_container
    image: dpage/pgadmin4
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: yaty1031@gmail.com
      PGADMIN_DEFAULT_PASSWORD: 12345678
      PGADMIN_LISTEN_PORT: 80
    ports:
      - "5050:80"
    links:
      - "db:pgsql-server"
    volumes:
      - pgadmin-data:/var/lib/pgadmin

  # swagger:
  #   image: swaggerapi/swagger-ui
  #   ports:
  #     - "8080:8080"
  #   expose:
  #     - "8080"
  #   environment:
  #     API_URL: http://127.0.0.1:3000/
  # Uncomment this if you want to persist the data.
volumes:
  db-data:
  pgadmin-data:
